{
  "name": "n-autoresearch",
  "version": "0.1.0",
  "private": true,
  "description": "Kubernetes-native autonomous ML/RL research platform",
  "license": "Apache-2.0",
  "scripts": {
    "build:k8s": "nix build .#k8s-manifests",
    "build:helm": "nix build .#helm-chart",
    "fmt:nix": "nix fmt",
    "workload:sync": "uv sync",
    "workload:smoke": "uv run experiments/train.py"
  }
}
