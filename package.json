{
  "name": "n-autoresearch",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous ML research infrastructure on iii-engine",
  "license": "Apache-2.0",
  "scripts": {
    "dev:orchestrator": "uv run python workers/orchestrator/orchestrator.py",
    "dev:gpu": "cd workers/gpu && cargo run --release",
    "prepare:data": "uv run experiments/prepare.py",
    "train": "uv run experiments/train.py"
  }
}
